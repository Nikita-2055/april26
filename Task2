#include<stdio.h>

int main(){
    int size = 3;
    int arr[size];
    int sum = 0;
    for(int i = 0; i < size; ++i){
        scanf("%d",&arr[i]);
    }
    for(int i = 0; i < size; ++i){
        sum += arr[i]; 
    }
    printf("result: %d\n",sum / size);
    return 0;
}
