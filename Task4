#include<stdio.h>

int main(){
    int size = 5;
    char str[size];
    scanf("%s",str);
    for(int i = 0; str[i] != '\0'; ++i){
        if(str[i] >= '0' && str[i] <= '9'){
            printf("%c",str[i]);
        }
    }
    return 0;
}
